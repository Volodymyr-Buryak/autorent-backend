package com.carhub.carrental.Fleet.domain.model.enums;

public enum CarBodyType {
    SEDAN,
    HATCHBACK,
    STATION_WAGON,
    SUV,
    CROSSOVER,
    MINIVAN,
    COUPE,
    CONVERTIBLE,
    PICKUP
}